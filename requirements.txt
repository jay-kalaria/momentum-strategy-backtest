# Web Framework
streamlit>=1.31.0

# Data Analysis
pandas>=2.0.0
yfinance>=0.2.35

# Dependencies that need specific versions to avoid conflicts
protobuf>=5.0.0,<6.0.0
opentelemetry-proto==1.34.1
opentelemetry-exporter-otlp-proto-grpc==1.34.1

# Optional but recommended for development
jupyter>=1.0.0  # Since you're using .ipynb files